export interface Video {
    id: string,
    slug: string;
    title: string;
    description: string;
    labelList: string[];
    body: string;
    link: string;
    createdAt: string;
    updatedAt: string;
}
